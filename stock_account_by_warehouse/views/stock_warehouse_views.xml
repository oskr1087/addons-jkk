<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_warehouse_form_stock_account_by_warehouse" model="ir.ui.view">
        <field name="name">stock.warehouse.form.stock.account.by.warehouse</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Contabilidad de Inventario" name="warehouse_inventory_accounting">
                    <group>
                        <group string="Configuración">
                            <field name="use_warehouse_stock_accounts"/>
                        </group>
                    </group>

                    <div class="alert alert-info mb-3" role="alert">
                        <strong>Configuración contable del almacén.</strong>
                        Al activar esta opción, los movimientos de inventario asociados
                        al almacén utilizarán las cuentas y el diario definidos en esta
                        pestaña. Si permanece desactivada, se utilizará la configuración
                        contable general del inventario.
                    </div>

                    <group invisible="not use_warehouse_stock_accounts">
                        <group string="Valoración">
                            <field name="warehouse_stock_valuation_account_id"
                                   options="{'no_create': True}"/>
                            <field name="warehouse_stock_journal_id"
                                   options="{'no_create': True}"/>
                        </group>
                        <group string="Entradas y salidas">
                            <field name="warehouse_stock_input_account_id"
                                   options="{'no_create': True}"/>
                            <field name="warehouse_stock_output_account_id"
                                   options="{'no_create': True}"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
